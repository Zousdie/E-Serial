<UserControl x:Class="E_Serial.ConnShow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:E_Serial"
             mc:Ignorable="d" 
             Width="auto"
             Height="auto" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox x:Name="txt_Data" Grid.Row="0" IsReadOnly="True" MaxLines="8000" ScrollViewer.VerticalScrollBarVisibility="Auto" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="19" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Background="#FF747474" IsReadOnly="True" Text=">" MaxLines="1" TextOptions.TextFormattingMode="Display" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
            <TextBox x:Name="txt_Write" IsEnabled="{Binding Status}" Grid.Column="1" Background="#FF747474" TextOptions.TextFormattingMode="Display" VerticalContentAlignment="Center" KeyDown="txt_Write_KeyDown" />
        </Grid>
    </Grid>
</UserControl>
